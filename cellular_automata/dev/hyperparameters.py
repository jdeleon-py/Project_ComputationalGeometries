# GLOBAL HYPERPARAMETERS CLASS

class Hyperparameters:
	'''
	'''
	WINDOW_WIDTH = 512
	WINDOW_HEIGHT = 512
	DIM = 128

	CELL_WIDTH = WINDOW_WIDTH / DIM
	CELL_HEIGHT = WINDOW_HEIGHT / DIM

	BLACK = (0, 0, 0)
	WHITE = (255, 255, 255)


if __name__ == "__main__": pass