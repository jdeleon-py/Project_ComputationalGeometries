# HYPERPARAMETERS CLASS

class Hyperparameters:
	'''
	- REF_CIR : allows the tool to draw a reference circle displaying the corresponding points
	- POINT_NUM: specifies the number of points to be drawn onto the cnavas
	- MULTIPLIER: specifies the times tables for the drawing tool to follow
	- DIM: specifies the size (DIM x DIM) on the drawing canvas (in pixels) 
	'''
	REF_CIR: bool = True
	POINT_NUM: int = 300
	MULTIPLIER: int = 2
	DIM: int = 600


if __name__ == "__main__": pass
